# ⚡ Быстрое решение: Сеть TravelPi не видна

## Вариант 1: Автоматическое исправление (рекомендуется)

Подключитесь к Raspberry Pi через SSH и выполните:

```bash
sudo ~/travelpi/scripts/fix-wifi.sh
```

Скрипт автоматически:
- Разблокирует Wi-Fi
- Перезапустит все службы
- Настроит интерфейсы
- Проверит результат

**Время выполнения: ~15 секунд**

## Вариант 2: Ручное исправление

```bash
# 1. Разблокировать Wi-Fi
sudo rfkill unblock wifi

# 2. Перезапустить hostapd
sudo systemctl restart hostapd

# 3. Перезапустить dnsmasq
sudo systemctl restart dnsmasq

# 4. Подождать 10 секунд
sleep 10

# 5. Проверить статус
sudo systemctl status hostapd
```

## Вариант 3: Диагностика

Если автоматическое исправление не помогло:

```bash
# Запустить диагностику
~/travelpi/scripts/diagnose.sh

# Просмотреть логи
sudo journalctl -u hostapd -n 50
```

## Вариант 4: Полная перезагрузка

```bash
sudo reboot
```

После перезагрузки подождите 1-2 минуты для полной инициализации.

## Проверка результата

На вашем телефоне/ноутбуке:
1. Откройте список Wi-Fi сетей
2. Найдите сеть **TravelPi**
3. Подключитесь с паролем: **TravelPi2024**
4. Откройте браузер: http://192.168.4.1

## Если ничего не помогло

См. подробное руководство: [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

Или создайте issue на GitHub с выводом команды:
```bash
~/travelpi/scripts/diagnose.sh > ~/diagnostics.txt
cat ~/diagnostics.txt
```

---

**В 90% случаев помогает простая перезагрузка!** 🔄
